import Button from "./Button"
import Display from "./Display"

const Section = ( props ) => {
    return (
        <div className="section">
           
        </div>
    )
}

export default Section
